import React from "react";

const NoMessage = ({ message }) => {
  return (
    <div className="w-full p-4 mt-4 text-center bg-gray-200 rouned-full">
      {message}
    </div>
  );
};

export default NoMessage;
